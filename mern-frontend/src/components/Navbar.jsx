
import React from 'react';

const Navbar = () => {
    return (
        <nav style={{ padding: '1rem', background: '#FFFF00', color: '#fff' }}>
            <h1>MERN Transactions Dashboard</h1>
        </nav>
    );
};

export default Navbar;
